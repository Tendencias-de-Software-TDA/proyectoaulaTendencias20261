{
  "version": 2,
  "functions": {
    "backend/backend/wsgi.py": {
      "runtime": "vercel-python@4.3.0",
      "maxDuration": 30
    }
  },
  "buildCommand": "python manage.py migrate --run-syncdb && python manage.py shell -c \"from django.contrib.auth import get_user_model; User = get_user_model(); User.objects.filter(email='admin@admin.com').exists() or User.objects.create_superuser(email='admin@admin.com', password='Admin1234!', username='admin')\"",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "backend/backend/wsgi.py"
    }
  ]
}